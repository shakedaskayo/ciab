apiVersion: v1
kind: Service
metadata:
  name: {{ include "ciab.fullname" . }}
  labels:
    {{- include "ciab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ciab.selectorLabels" . | nindent 4 }}
